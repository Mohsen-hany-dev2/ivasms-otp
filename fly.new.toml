app = "ivasms-cli"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile.fly"

[processes]
  app = "python main.py"

[[mounts]]
  source = "ivasms_data"
  destination = "/app/data"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512
